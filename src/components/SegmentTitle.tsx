function SegmentTitle(props:{text:string}) {
  return (
    <div className="col s12 abalible-services-title-container">
      <h1 className="part-title">{props.text}</h1>
    </div>
  );
}

export default SegmentTitle;
